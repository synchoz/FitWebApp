{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run migrate",
    "start": "node app.js",
    "db:create": "sequelize-cli db:create",
    "setup": "npm run db:create && npm run migrate",
    "migrate": "sequelize-cli db:migrate",
    "migrate:undo": "sequelize-cli db:migrate:undo",
    "migrate:undo:all": "sequelize-cli db:migrate:undo:all"
  },
  "engines": {
    "node": ">=16.16.0 <17.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cloudinary": "^1.41.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-fileupload": "^1.5.0",
    "multer": "^1.4.5-lts.1",
    "mysql": "^2.18.1",
    "mysql2": "^3.5.0",
    "nodemon": "^3.0.1",
    "path": "^0.12.7",
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6",
    "streamifier": "^0.1.1"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.5"
  }
}
